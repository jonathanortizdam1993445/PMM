<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimaryDark">@color/dark</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:textColorTertiary">@color/tertiary_text</item>

        <item name="android:colorActivatedHighlight">@color/light_color</item>
        <item name="android:colorPressedHighlight">@color/light_color</item>
        <item name="android:colorLongPressedHighlight">@color/light_color</item>
        <item name="android:colorControlHighlight">@color/light_color</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

</resources>
